<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wanderlust</title>
    <!-- Link to Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Additional custom styles */
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f7f7f7;
            margin: 0;
            padding: 0;
        }

        .navbar {
            border-bottom: 2px solid #e1e1e1;
            padding: 10px 0; /* Reduced padding to make navbar smaller */
        }

        .navbar .navbar-brand img {
            height: 30px; /* Reduced the logo size */
        }

        .navbar-nav .nav-item .nav-link {
            font-size: 14px; /* Reduced font size for navbar links */
        }

        /* Main Title Section */
        .title-display {
            text-align: center;
            padding-top: 40px;
            padding-bottom: 40px;
            background-color: #ffffff;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            margin-top: 100px; /* Adjusted for fixed navbar space */
        }

        .title-display h2 {
            font-size: 2.5rem;
            font-weight: bold;
            color: #2c3e50;
            margin-bottom: 5px;
        }

        .title-display p {
            font-size: 1.1rem;
            color: #7f8c8d;
            margin-bottom: 30px;
        }

        /* Buttons Styling */
        .btn-primary, .btn-outline-primary {
            border-radius: 50px;
            padding: 10px 25px;
            font-size: 15px;
        }

        .btn-primary {
            background-color: #4CAF50;
            border-color: #4CAF50;
        }

        .btn-primary:hover {
            background-color: #45a049;
            border-color: #45a049;
        }

        .btn-outline-primary {
            border-color: #4CAF50;
            color: #4CAF50;
        }

        .btn-outline-primary:hover {
            background-color: #4CAF50;
            color: white;
        }

        /* Responsive Container and Text */
        .container {
            max-width: 1200px;
            padding: 15px;
        }

        /* Adjusting for mobile responsiveness */
        @media (max-width: 768px) {
            .title-display h2 {
                font-size: 2rem;
            }

            .title-display p {
                font-size: 1rem;
            }

            .navbar-nav .nav-item .nav-link {
                font-size: 12px;
            }
        }
    </style>
</head>
<body>
    <!-- Navbar Section (Fixed at the top) -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top shadow-sm">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">
                <img class="airbnb-logo" src="https://s3-us-west-2.amazonaws.com/s.cdpn.io/2522641/logo.png" alt="Airbnb Logo">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/listing">Listings</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/listing/new">Create Listing</a>
                    </li>
                </ul>

                <!-- Buttons for Sign Up, Login, and Logout -->
                <ul class="navbar-nav ms-3">
                    <% if (!user) { %> <!-- Check if user is NOT logged in -->
                        <li class="nav-item">
                            <a class="btn btn-outline-primary btn-sm" href="/signup">Sign Up</a>
                        </li>
                        <li class="nav-item ms-2">
                            <a class="btn btn-primary btn-sm" href="/login">Login</a>
                        </li>
                    <% } else { %> <!-- If the user is logged in -->
                        <li class="nav-item">
                            <a class="btn btn-danger btn-sm" href="/logout">Logout</a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content for Home Page -->
    <div class="container py-5 mt-5">
        <div class="row">
            <div class="col-md-8 offset-md-2">
                <div class="title-display">
                    <h2>Welcome to Wanderlust</h2>
                    <p>Discover new places, plan your trips, and experience the world like never before!</p>
                    
                    <!-- Check if user is signed up but not logged in -->
                    <% if (!user) { %> <!-- User is NOT logged in -->
                        <div class="mt-4">
                            <a href="/signup" class="btn btn-primary">Sign Up</a>
                        </div>
                    <% } else { %> <!-- User is logged in -->
                        <div class="mt-4">
                            <a href="/listing" class="btn btn-primary">Browse Listings</a>
                        </div>
                    <% } %>

                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap 5 JS and Popper.js for responsive navbar -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
</body>
</html>
