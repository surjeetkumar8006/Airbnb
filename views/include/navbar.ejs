<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Wanderlust</title>
    <!-- Link to Bootstrap 5 CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      /* Global Styles */
      body {
        font-family: "Poppins", sans-serif;
        background-color: #f4f4f4;
        margin: 0;
        padding: 0;
        color: #333;
      }

      /* Navbar Styling */
      .navbar {
        background: rgba(255, 255, 255, 0.9);
        border-bottom: 1px solid rgba(0, 0, 0, 0.1);
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      }

      .navbar .nav-link {
        font-size: 16px;
        font-weight: 500;
        color: #333 !important;
        transition: color 0.3s;
      }

      .navbar .nav-link:hover {
        color: #007bff !important;
      }

      .navbar-brand img {
        height: 40px;
      }

      /* Hero Section */
      .hero-section {
        text-align: center;
        padding: 100px 20px;
        background: linear-gradient(135deg, #007bff, #6610f2);
        color: white;
        border-radius: 10px;
        margin-top: 80px;
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        animation: fadeIn 1s ease-in-out;
      }

      .hero-section h1 {
        font-size: 3rem;
        font-weight: bold;
      }

      .hero-section p {
        font-size: 1.2rem;
        opacity: 0.9;
      }

      /* Buttons Styling */
      .btn-custom {
        padding: 12px 30px;
        border-radius: 30px;
        font-size: 16px;
        font-weight: 600;
        transition: all 0.3s;
      }

      .btn-primary {
        background-color: #007bff;
        border: none;
      }

      .btn-primary:hover {
        background-color: #0056b3;
      }
      .btn-custom {
        padding: 10px 20px;
        border-radius: 30px;
        font-size: 16px;
        font-weight: 600;
        transition: all 0.3s;
      }

      .btn-success {
        background-color: #28a745;
        border: none;
      }

      .btn-success:hover {
        background-color: #218838;
      }

      .btn-outline-primary {
        border-color: #007bff;
        color: #007bff;
      }

      .btn-outline-primary:hover {
        background-color: #007bff;
        color: white;
      }

      /* Keyframe Animation */
      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(-20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      /* Responsive Adjustments */
      @media (max-width: 768px) {
        .hero-section h1 {
          font-size: 2.5rem;
        }

        .hero-section p {
          font-size: 1rem;
        }
      }
    </style>
  </head>
  <body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-light fixed-top">
      <div class="container">
        <a class="navbar-brand" href="/">
          <img
            src="https://s3-us-west-2.amazonaws.com/s.cdpn.io/2522641/logo.png"
            alt="Logo"
          />
        </a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
            <li class="nav-item">
              <a class="nav-link" href="/listing">Listings</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/listing/new">Create Listing</a>
            </li>
          </ul>
          <ul class="navbar-nav ms-3">
            <% if (!user) { %>
            <li class="nav-item">
              <a class="btn btn-outline-primary btn-custom" href="/signup"
                >Sign Up</a
              >
            </li>
            <li class="nav-item ms-2">
              <a class="btn btn-primary btn-custom" href="/login">Login</a>
            </li>
            <% } else { %>
            <li class="nav-item">
              <a class="btn btn-success btn-custom" href="/dashboard"
                >Dashboard</a
              >
            </li>
            <li class="nav-item ms-2">
              <a class="btn btn-danger btn-custom" href="/logout">Logout</a>
            </li>
            <% } %>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Hero Section -->
    <div class="container">
      <div class="hero-section">
        <h1>Welcome to Wanderlust</h1>
        <p>
          Discover new places, plan your trips, and experience the world like
          never before!
        </p>
        <a href="/listing" class="btn btn-primary btn-custom"
          >Explore Listings</a
        >
      </div>
    </div>

    <!-- Bootstrap 5 JS -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
  </body>
</html>
