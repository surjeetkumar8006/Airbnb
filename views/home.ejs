<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to Wanderlust</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f2f2f2;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        header {
            background-color: #34495e;
            color: white;
            padding: 30px 0;
            text-align: center;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            position: relative;
        }

        header h1 {
            font-size: 36px;
            margin: 0;
            letter-spacing: 2px;
        }

        main {
            text-align: center;
            padding: 50px 20px;
        }

        footer {
            background-color: #34495e;
            color: white;
            padding: 15px 0;
            text-align: center;
            position: fixed;
            width: 100%;
            bottom: 0;
            box-shadow: 0 -4px 8px rgba(0, 0, 0, 0.1);
        }

        .message {
            font-size: 24px;
            font-weight: 600;
            color: #333;
            margin-bottom: 30px;
        }

        .content p {
            font-size: 18px;
            line-height: 1.6;
            color: #555;
            margin-bottom: 30px;
        }

        .btn {
            padding: 12px 25px;
            font-size: 18px;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 30px;
            cursor: pointer;
            text-decoration: none;
            transition: background-color 0.3s ease;
            margin: 5px;
        }

        .btn:hover {
            background-color: #45a049;
        }

        .btn:active {
            background-color: #388e3c;
        }

        /* Flash messages */
        .flash-success {
            padding: 15px;
           
            color: rgb(0, 0, 0);
            margin-top: 20px;
            border-radius: 5px;
            font-size: 16px;
            text-align: center;
        }

        .flash-error {
            padding: 15px;
            background-color: #f44336;
            color: white;
            margin-top: 20px;
            border-radius: 5px;
            font-size: 16px;
            text-align: center;
        }

        /* Responsive styles */
        @media (max-width: 768px) {
            .message {
                font-size: 20px;
            }
            .btn {
                font-size: 16px;
                padding: 10px 20px;
            }
        }
    </style>
</head>
<body>

    <header>
        <h1>Welcome to Wanderlust</h1>
    </header>

    <main>
        <div class="message">
            <p>Hi, I am root! Welcome to Wanderlust</p>
        </div>

        <div class="content">
            <p>Discover new places, find the best destinations, and start your journey today!</p>
            <a href="/listing" class="btn">Browse Listings</a>

            <!-- Flash messages (if any) -->
            <% if (loginSuccess) { %>
                <div class="flash-success"><%= loginSuccess %></div>
            <% } %>
            <% if (signupSuccess) { %>
                <div class="flash-success"><%= signupSuccess %></div>
            <% } %>
            
            <!-- Conditionally show login/logout buttons -->
            <% if (user) { %> <!-- If user is logged in -->
                <a href="/logout" class="btn">Logout</a>
            <% } else { %> <!-- If user is not logged in -->
                <a href="/login" class="btn">Login</a>
                <a href="/signup" class="btn">Sign Up</a>
            <% } %>
        </div>
    </main>

    <footer>
        <p>© 2025 Wanderlust. All rights reserved.</p>
    </footer>

</body>
</html>
