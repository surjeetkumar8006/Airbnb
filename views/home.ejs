<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wanderlust - Explore the World</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            background: linear-gradient(to right, #141E30, #243B55);
            color: #fff;
            text-align: center;
            overflow-x: hidden;
        }

        header {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            padding: 15px 50px;
            position: fixed;
            width: 100%;
            top: 0;
            left: 0;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 1000;
            box-shadow: 0 0 10px rgba(255, 255, 255, 0.2);
        }

        header h1 {
            font-size: 24px;
            font-weight: 600;
            letter-spacing: 1px;
        }

        nav {
            display: flex;
            align-items: center;
        }

        nav a {
            color: white;
            text-decoration: none;
            margin: 0 15px;
            font-weight: 400;
            transition: 0.3s;
            padding: 5px 10px;
            border-radius: 5px;
        }

        nav a:hover {
            background: rgba(255, 255, 255, 0.2);
            color: #f1c40f;
        }

        .hero {
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            padding: 50px;
            text-align: center;
            animation: fadeIn 1.5s ease-in-out;
        }

        .hero h2 {
            font-size: 42px;
            font-weight: 700;
            text-shadow: 2px 2px 10px rgba(255, 255, 255, 0.3);
        }

        .hero p {
            font-size: 20px;
            margin: 20px 0;
            opacity: 0.9;
            max-width: 600px;
        }

        .btn {
            display: inline-block;
            padding: 14px 30px;
            background: #f1c40f;
            color: black;
            font-weight: 600;
            text-decoration: none;
            border-radius: 30px;
            transition: all 0.3s ease-in-out;
            box-shadow: 0 5px 10px rgba(241, 196, 15, 0.5);
            font-size: 18px;
        }

        .btn:hover {
            background: #e1b700;
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(241, 196, 15, 0.6);
        }

        footer {
            background: rgba(255, 255, 255, 0.1);
            padding: 15px;
            position: fixed;
            bottom: 0;
            width: 100%;
            font-size: 14px;
            backdrop-filter: blur(10px);
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body>

<header>
    <h1>Wanderlust</h1>
    <nav>
        <a href="/listing">Explore</a>
        <% if (user) { %>
            <a href="/dashboard">Dashboard</a>
            <a href="/logout">Logout</a>
        <% } else { %>
            <a href="/login">Login</a>
            <a href="/signup">Sign Up</a>
        <% } %>
        
    </nav>
</header>

<section class="hero">
    <h2>Explore the Beauty of the World</h2>
    <p>Find breathtaking destinations, plan your dream trips, and book stays effortlessly.</p>
    <a href="/listing" class="btn">Start Exploring</a>
</section>

<footer>
    <p>&copy; 2025 Wanderlust. All rights reserved.</p>
</footer>

</body>
</html>